import { MainRoutes } from './MainRoutes';
import { PrivateRoutes } from './PrivateRoutes';
import {
    RoutesListPaths,
    PrivateRoutesList,
    SidebarRoutes,
    PublicRoutesList,
    getSidebarRoutesForRole,
} from './RoutesList';

export {
    MainRoutes,
    PrivateRoutes,
    RoutesListPaths,
    PrivateRoutesList,
    SidebarRoutes,
    PublicRoutesList,
    getSidebarRoutesForRole,
};

export default {
    MainRoutes,
    PrivateRoutes,
    RoutesListPaths,
    PrivateRoutesList,
    SidebarRoutes,
    PublicRoutesList,
};
